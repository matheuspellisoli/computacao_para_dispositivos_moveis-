<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.pro.pellisoli.todo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppFilmes">
        <activity android:name="br.pro.br.com.pellisoli.appfilmes.FormularioActivity"></activity>
        <activity
            android:name="br.pro.br.com.pellisoli.appfilmes.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppFilmes.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>